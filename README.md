## BackEnd NestJS App "Kryvyi Rih City Guide"

### Install and run:

1. `cp .env.sample .env` and fill it in with the required credentials
2. `yarn install`
3. `yarn start`
4. Navigate to [http://localhost:3000/](http://localhost:3000/)
